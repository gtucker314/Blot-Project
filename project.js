const width = 125;
const height = 125;

setDocDimensions(width, height);

const finalLines = [];


function createCircle(radius) {
  const t = new bt.Turtle()
  t.arc(360, 5)
  console.log("circle")
  return t.path
}

// let randomShape = new bt.randIntInRange (1,2)
//let shape = []
//if (randomShape == 1) {
//shape = createCircle(bt.randIntInRange(10, 50)
}
//else {
//shape = createRec(bt.randIntInRange(10,50)
}

//return shape 